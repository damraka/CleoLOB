# Research reproduction report

- Source commit and whether the working tree was dirty:
- OS, CPU, Python version, package versions (`python -m pip freeze`):
- Exact command and relative config path:
- Config SHA-256 and seed policy:
- Dataset provider, symbol, period, license/access conditions and SHA-256:
- Evidence artifact and verification command/output:
- Expected result, metric definition and acceptable numerical tolerance:
- Observed result (including FAIL/WARNING/INVALID outcomes):
- Differences in environment or procedure:
- Large artifacts available separately and their checksums:

Remove user names, credentials and absolute personal paths before submitting.
Distinguish byte-level artifact verification from independent reproduction and
from scientific validation of the model's real-market assumptions.
